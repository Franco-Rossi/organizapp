const express = require('express');
const app = express();

// SETTINGS

// MIDDLEWARES

// ROUTES

// STATIC FILES

// SERVER START
app.listen(3000, () => {
    console.log('Server on port 3000');
});
